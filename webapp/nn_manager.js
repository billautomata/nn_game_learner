module.exports = function nn_manager (options) {

  // loads nn from localStorage
  // draws nn
  // create new nn
  // control layer information in new nn
  // control mutation rate
  // has flag for ticking multiple times
  // has flag for not drawing
  // trains neural network on recorded input frames
  // scores nn by loading state and running simulation until mario dies once

}
